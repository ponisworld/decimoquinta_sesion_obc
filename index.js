const nombre = 'Andrea';
const apellido = 'Bellver';

const miObj = {
    nombre: nombre,
    apellido: apellido,
}

// sessionStorage.setItem("persona", JSON.stringify(miObj));

// localStorage.setItem("persona", JSON.stringify(miObj));

// document.cookie = "persona=" + JSON.stringify(miObj) + ";max-age=120";